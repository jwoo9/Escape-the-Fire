Basically what this program is supposed to do is when you pinpoint the user at a certain spot it checks which path set leads them to the exit.
It's supposed to work as follows:

1. Open navigator.html 
2. Hover the mouse around the map, and when you want to pinpoint yourself in a spot, click P

What its next supposed to do (doesn't work yet) is then identify which set of paths the user can take to reach an exit.

Its supposed to do this by:

-  checking which room shares the same coordinates as the pinpoint and return that room (which works)
- Identify which arrow's tail (i.e. the start of a path) shares the same room as such and display that (which doesn't work)
- Displaying only the set of paths of which that arrow is part (doesn't work)
- Hiding other path sets (doesn't work yet either)
